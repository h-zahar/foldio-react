import Icon from "../assets/icons/settings-icon.svg";

const SettingsIcon = () => {
  return (
    <div>
      <img src={Icon} width={30} height={30} />
    </div>
  );
};

export default SettingsIcon;
